/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['upload.wikimedia.org', 'w7.pngwing.com', 'assets.stickpng.com'],
      },
}

module.exports = nextConfig
